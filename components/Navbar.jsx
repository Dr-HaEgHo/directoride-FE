import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Navbar = () => {
  return (
    <View style={a.container} > 
      <Text>Welcome to the nav</Text>
    </View>
  )
}

export default Navbar

const styles = StyleSheet.create({
    
})